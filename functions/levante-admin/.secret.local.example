# Copy to .secret.local next to package.json (same folder as this example), not src/.
# Value is your Resend API key (re_...). Do not commit .secret.local.

CREATE_USERS_EXPORT_SMTP_PASS=re_REPLACE_WITH_YOUR_RESEND_API_KEY
